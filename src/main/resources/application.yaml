spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tasksq
    username: postgres
    password: 1212
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration